# Sprint 2 / Phase 1 Guardrails

These guardrails apply to ParaMetrics Phase 1 / Sprint 2 work.

## State

Sprint 1 / Phase 0 stabilization is complete. The proof pack is recorded in `docs/proof/sprint-1-phase-0-proof-pack.md`.

ParaMetrics remains a Google Business Profile first operations app. The target product direction is still a multi-tenant, multi-channel SaaS, but Phase 2 integrations remain blocked until the Phase 1 foundation is intentionally built and verified.

## Active Priority

Phase 1 / Sprint 2 starts with report service MVP work, then workspace/member foundation work.

Current Sprint 2 task:

- S2-01 report service abstraction complete: pure backend report run metadata helpers for dashboard snapshot inputs are in place.
- S2-02 PDF output generation complete: minimal backend PDF buffers from S2-01 report run metadata are in place without routes, persistence, queues, or frontend changes.
- S2-03 XLSX output generation complete: minimal backend XLSX buffers from S2-01 report run metadata are in place without routes, persistence, queues, or frontend changes.
- S2-04 report/report_runs persistence complete: report definitions and report run lifecycle metadata persistence are in place without routes, queues, scheduler changes, or frontend wiring.
- S2-04.1 report index verification complete: configured MongoDB index creation for `reports` and `report_runs` was verified before adding report routes.
- S2-05 authenticated dashboard snapshot report route complete: existing report metadata, PDF/XLSX output, persistence, auth, rate-limit, and audit services are wired without queues, scheduler changes, frontend wiring, or file storage.
- S2-05.1 authenticated report route smoke complete: live API smoke verified HTTP 200, PDF/XLSX base64 response, metadata-only `report_runs` persistence, and audit success logging.
- S2-06 frontend dashboard report action complete: the GBP dashboard calls the authenticated backend dashboard snapshot report route and downloads returned PDF/XLSX base64 files without storing generated file content.
- S2-06.1 frontend report browser smoke complete: browser verification passed against the running API, including downloads, metadata-only `report_runs` persistence, and audit success logging.
- S2-07 workspace/member foundation audit and design complete: existing org/user/client/location ownership is documented before any workspace/member implementation.
- S2-07.1 local dev port coordination and app-shell cleanup complete: deterministic local API/web port preparation and a cleaner global header/page-action split are in place without workspace/member runtime changes or Phase 2 integrations.
- S2-08 organization_members indexes and owner seed migration complete: membership collection indexes and a dry-run-first owner membership seed migration were added without route authorization changes.
- S2-08.1 organization_members owner seed migration apply complete: safe owner memberships were applied and verified against live Mongo with summarized counts only.
- S2-09 organization access helpers complete: membership-aware helper functions were added without route authorization changes, RBAC middleware, auth/JWT changes, frontend UI, or Phase 2 integrations.
- S2-10 org/report membership authorization complete: low-blast-radius org/report route checks are in place while auth/JWT behavior, frontend UI, member APIs, and Phase 2 integrations remain unchanged.
- S2-10.1 location-bound GBP membership authorization complete: current GBP location-bound operations are protected after existing owned-location and canonical scope checks, without changing Google provider auth, app JWT middleware, frontend workspace/member UI, member APIs, billing, or Phase 2 integrations.
- S2-10.2 GBP membership smoke verification complete: live local API/Mongo smoke proof for S2-10.1 is recorded in `docs/proof/s2-10-2-gbp-membership-smoke.md`.
- S2-11 new organization owner membership creation complete: brand-new org creation creates or preserves an idempotent active owner membership for the authenticated creator before returning success.
- S2-11.1 new organization owner membership smoke verification complete: live local API/Mongo smoke proof for S2-11 is recorded in `docs/proof/s2-11-1-new-org-owner-membership-smoke.md`.
- S2-12 read-only organization member listing complete: authenticated owner/admin/manager members can list sanitized organization membership rows without member mutations, invites, frontend UI, or Phase 2 scope.
- S2-12.1 read-only organization member listing smoke verification complete: live local API/Mongo smoke proof for S2-12 is recorded in `docs/proof/s2-12-1-read-only-member-listing-smoke.md`.
- S2-13 workspace/member foundation proof pack and hardening audit complete: Sprint 2 workspace/member implementation, proof, access matrix, risks, and next task guidance are recorded in `docs/proof/sprint-2-workspace-member-foundation-proof-pack.md`.
- S2-13.1 proof pack GPT decision correction complete: the Sprint 2 workspace/member foundation proof pack records `GPT decision: Pass`.
- S2-14 member-management API contract and fixture strategy complete: future member mutation endpoints, role rules, last-owner protection, invite deferral, fixtures, tests, and non-goals are documented without runtime behavior changes; proof is recorded in `docs/proof/s2-14-member-management-api-contract.md`.
- S2-15 controlled membership fixture seed/audit complete: dry-run-first local fixture workflow for owner/admin/manager/viewer/member/invited/disabled memberships exists without route behavior changes or Phase 2 scope.
- S2-15.2 organization member fixtures apply complete: controlled fixture organization and seven fixture memberships were applied and post-apply dry-run verified zero remaining backfillable fixture memberships.
- S2-16 direct member-management API implementation complete: existing `user_id` based create/update/disable member routes are in place under the authenticated org route without email invitations, frontend UI, auth/JWT changes, provider changes, or Phase 2 scope.
- S2-16.1 direct member-management API smoke verification complete: live local API/Mongo smoke proof for S2-16 is recorded in `docs/proof/s2-16-1-member-management-api-smoke.md`. A thin Claude Code governance adapter (`CLAUDE.md`, `docs/claude-code/README.md`) is also in place; `docs/codex/*` remains the source of truth.
- S2-17 workspace member management UI complete: a minimal authenticated frontend page `/organization-members` wires the verified member-management APIs without changing backend behavior, adding email invitations, or installing dependencies; proof is recorded in `docs/proof/s2-17-workspace-member-ui.md`.
- S2-17.1 workspace member UI browser smoke complete: local API + web dev-server smoke verified the SPA shell serves `/organization-members`, the dev-served page module carries the documented direct-user_id and no-invitation copy, the AppShell nav includes the `Members` item, and the exact API endpoints called by the page return the expected status/sanitization/idempotency/denial shapes. Proof is recorded in `docs/proof/s2-17-1-workspace-member-ui-browser-smoke.md`.
- S2-18 Sprint 2 / Phase 1 closeout proof pack complete: docs-only audit summarizing report foundation (S2-01..S2-06.1) and workspace/member foundation (S2-07..S2-17.1), with security/tenancy, test/build proof, remaining risks, and conservative next tasks. Proof is recorded in `docs/proof/sprint-2-closeout-proof-pack.md`. Phase 2 integrations remain blocked pending explicit acceptance of this closeout.
- S2-19 API `npm test` script consolidation complete: `apps/api/package.json` exposes a deterministic `test` script that runs the same focused `node --test` matrix already verified in S2-18 without changing test behavior, application code, dependencies, or worker/scheduler runtime. Proof is recorded in `docs/proof/s2-19-api-test-script.md`. Phase 2 integrations remain blocked.
- S2-20 report history and durable output storage contract complete: docs-only design for the future `GET /api/v1/reports/runs`, `GET /api/v1/reports/runs/:runId`, `GET /api/v1/reports/runs/:runId/outputs/:format`, optional future regenerate, `ReportStorageAdapter` abstraction, additive `report_runs.outputs[]` fields, `organization_members`-based authorization with owner/admin/manager/viewer read access and `member` denial, `/reports/history` frontend UX, listing/download indexes, audit/rate-limit buckets, and a conservative implementation sequence (S2-22 storage → S2-23 listing → S2-24 download → S2-25 frontend; S2-26 queue/worker only after acceptance). Contract is recorded in `docs/architecture/report-history-and-storage.md`; proof is recorded in `docs/proof/s2-20-report-history-storage-contract.md`. Phase 2 integrations remain blocked.
- S2-22 durable local report output storage adapter complete: `apps/api/src/services/reportStorage.js` provides a `local` adapter with safe key building, traversal-protected resolution, sha256 checksums, and `writeOutput`/`readOutput`/`statOutput`/`deleteOutput`. `POST /api/v1/reports/dashboard-snapshot` now writes successful PDF/XLSX outputs to the adapter and persists `storage_provider`/`storage_key`/`content_type`/`filename`/`checksum`/`generated_at`/`expires_at` on `report_runs.outputs[]` while keeping `path` null and the existing base64 `files[]` response unchanged. Storage-write failures mark the output and run failed compactly. Storage root defaults to `REPORT_STORAGE_LOCAL_DIR` when set, else `<os.tmpdir()>/parametrics/report-outputs`. No new dependencies were added; `package-lock.json` is unchanged. The `apps/api` `npm test` script now also runs `src/services/reportStorage.test.js`. Proof is recorded in `docs/proof/s2-22-durable-local-report-storage.md`. Listing/download routes and cloud adapters remain future. Phase 2 integrations remain blocked.
- S2-22.1 durable local report storage live smoke verification complete: a live local API + Mongo smoke under the controlled `s2-15-fixture-org` scope confirmed HTTP 200 from `POST /api/v1/reports/dashboard-snapshot`, `report_run.status: succeeded`, two outputs with the full durable metadata set (`storage_provider`, `storage_key`, `content_type`, `filename`, `size`, `checksum: sha256`, `generated_at`, `expires_at: null`), `path: null`, base64 `files[]` still returned for compatibility, no absolute path or env value leaked in the response, files on disk under `/tmp/parametrics-s2-22-1-report-storage` with byte sizes and sha256 hashes matching the persisted metadata, persisted `report_runs` row with no `input_snapshot`, no raw buffer/base64 fields, and `location_org_map` untouched. Only the API runtime was started; workers and scheduler were not started. Proof is recorded in `docs/proof/s2-22-1-durable-report-storage-live-smoke.md`. Phase 2 integrations remain blocked.
- S2-23 read-only report run listing API complete: `GET /api/v1/reports/runs` returns sanitized `report_runs` rows for the requested `organization_id`, with optional `client_id`/`location_id`/`report_type`/`report_key`/`status`/`date_from`/`date_to` filters, `limit` bounded to `[1,100]` (default `25`), server-controlled `created_at desc` sort with `id` tiebreaker, and `{ limit, has_more, next_cursor: null }` pagination. Authorization uses `organization_members` only — `owner`/`admin` see all runs in the org; `manager`/`viewer` must supply a `client_id` or `location_id` matching their assignments; `member`/`invited`/`disabled`/missing denied. Sanitization omits Mongo `_id`, raw `input_snapshot`, raw buffers/base64, and absolute paths; `storage_key` is exposed as durable metadata per the S2-20 contract and no download route exists yet. Backend service helpers `listReportRuns`, `buildReportRunListQuery`, `sanitizeReportRunRow`, `REPORT_LIST_DEFAULT_LIMIT`, `REPORT_LIST_MAX_LIMIT` added to `apps/api/src/services/reportStore.js`. No new dependencies; `package-lock.json` unchanged; the focused `npm test` script needed no update. Proof is recorded in `docs/proof/s2-23-report-runs-listing-api.md`. Detail and download routes remain future (S2-24); the report history frontend remains future (S2-25). Phase 2 integrations remain blocked.
- S2-23.1 report run listing API live smoke verification complete: a live local API + Mongo smoke under the controlled `s2-15-fixture-org` scope confirmed HTTP 200 from `GET /api/v1/reports/runs`, the documented `{ report_runs[], pagination: { limit, has_more, next_cursor: null } }` shape, server-controlled newest-first sort, visibility of the S2-22.1 smoke row (`s2-22-1-smoke-dashboard`), filter narrowing under `status=succeeded&report_type=dashboard_snapshot&report_key=s2-22-1-smoke-dashboard&date_from=2026-05-11&date_to=2026-05-13&limit=1`, the full durable output metadata set per row (`storage_provider`, `storage_key`, `content_type`, `filename`, `size`, `checksum: sha256`, `generated_at`, `expires_at: null`, `path: null`), no `_id`, no `input_snapshot`, no `buffer`/`base64`, and no absolute server path in the response. Denial coverage exercised manager/viewer without scope (`403 organization_scope_required`), manager/viewer with assigned `client_id` (`200 ok:0`, because the smoke row has `client_id: null`), `member` (`403 organization_role_required`), `invited` (`403 organization_membership_required`), and `disabled` (`403 organization_membership_required`); JWT `role` was the default `"individual"` and authorization came from `organization_members` only. Mongo counts (`report_runs.report_key=s2-22-1-smoke-dashboard` = `1`, `report_runs.organization_id=s2-15-fixture-org` = `1`, `location_org_map.organization_id=s2-15-fixture-org` = `0`) match the listing API response. Only the API runtime was started; workers and scheduler were not started, and the API was stopped after the smoke. No backend or frontend source code, no test files, no `package.json`, and no `package-lock.json` were changed. Proof is recorded in `docs/proof/s2-23-1-report-runs-listing-live-smoke.md`. Phase 2 integrations remain blocked.
- S2-24 read-only report output download API complete: `GET /api/v1/reports/runs/:runId/outputs/:format` reads bytes through the existing `ReportStorageAdapter` and streams them back as a raw response body (not JSON, not base64). Auth uses `organization_members` only — `owner`/`admin` can download any output for runs in their organization; `manager`/`viewer` can download only when the run's `client_id`/`location_id` matches their `assigned_client_ids`/`assigned_location_ids`; org-level runs are denied for `manager`/`viewer` with `403 organization_scope_required`; `member`/`invited`/`disabled`/missing denied. Output readiness requires `status: succeeded` plus `storage_provider` and `storage_key` (otherwise `409 report_output_not_ready`). Missing run ⇒ `404 report_run_not_found`; missing output format ⇒ `404 report_output_not_found`; invalid format/runId ⇒ `400 bad_request`. Storage read failures surface as `500 report_output_read_failed`; size or sha256 mismatch surfaces as `500 report_output_integrity_failed`. Response headers set: `Content-Type` from persisted output, `Content-Disposition: attachment; filename="<sanitized>"`, `Content-Length`, `Cache-Control: no-store`, `X-Content-Type-Options: nosniff`. The route never returns base64, never returns absolute paths, never logs the storage root, and never mutates `report_runs`. New backend service helpers `getReportRunById` and `findReportRunOutput` added to `apps/api/src/services/reportStore.js`. No new dependencies; `package-lock.json` unchanged; the focused `npm test` script already includes the changed test files. Proof is recorded in `docs/proof/s2-24-report-output-download-api.md`. Optional regenerate route remains design-only; the frontend `/reports/history` page remains future (S2-25). Phase 2 integrations remain blocked.
- S2-25.1 report history UI browser smoke verification complete: a local API + web dev-server smoke under the controlled `s2-15-fixture-org` scope confirmed `GET /api/v1/health` returned `200`, the Vite dev server returned the SPA shell with `200` for both `/` and `/reports/history`, and the dev-served React modules carried the documented wiring (the new authenticated route in `apps/web/src/App.jsx`, the `Reports` → `/reports/history` nav entry in `apps/web/src/components/AppShell.jsx`, and the page module `apps/web/src/pages/ReportHistory.jsx` importing `listReportRunsForUser`/`downloadReportOutput`/`formatBytes`/`REPORT_RUN_STATUSES` from the new helper module and rendering the documented `/tmp` warning copy). `GET /api/v1/reports/runs?organization_id=s2-15-fixture-org` returned the documented `{ report_runs, pagination: { limit, has_more, next_cursor: null } }` shape with both fixture rows (S2-22.1 + S2-24.1) visible to `owner`. The documented filter combo (`status=succeeded&report_type=dashboard_snapshot&report_key=s2-24-1-smoke-dashboard&date_from=2026-05-15&date_to=2026-05-17&limit=1`) narrowed to the S2-24.1 row. `GET /api/v1/reports/runs/:runId/outputs/{pdf,xlsx}` under the owner JWT returned raw bytes (PDF=2047 / sha256 `fc62c1bb2b92...`; XLSX=8678 / sha256 `6c1331a5967c...`) with `Content-Type` matching the persisted `output.content_type`, `Content-Disposition: attachment; filename="..."` ASCII-safe, matching `Content-Length`, `Cache-Control: no-store`, and `X-Content-Type-Options: nosniff`; the page's `parseContentDispositionFilename` + `safeDownloadFilename` helpers were re-run against the captured headers and produced the same allow-listed filenames. The page module contained zero `storage_key` references; the helper module had a single presence-only check used for the `downloadable` computation. The listing response contained no `_id`/`input_snapshot`/`buffer`/`base64`/`/tmp/`/`/var/www/`/`REPORT_STORAGE_LOCAL_DIR`/storage-root literal/`@`-style address leakage. Error/denial coverage exercised `400 bad_request` (listing without `organization_id` and download with `outputs/csv`), `403 organization_role_required` (`member` listing), `403 organization_scope_required` (`manager` listing without scope and `manager` download on the org-level run), and `401 unauthorized` (no-auth download probe). JWT `role` was the default `"individual"` and authorization came from `organization_members` only. Interactive in-browser DOM mount and `<a download>` click were skipped because no headless-browser tool was available in this environment; the same `fetch` URL/header contract the page builds was reproduced and validated, and the existing 28-test helper suite covers the helper behaviors used by the page. Only the API runtime and the Vite dev server were started; workers and scheduler were not, and both processes were stopped after the smoke (the Vite child required a direct `kill <pid>` step because the workspace-name `pkill` did not match the Vite binary). No backend or frontend source code, no test files, no `package.json`, and no `package-lock.json` were changed. Proof is recorded in `docs/proof/s2-25-1-report-history-ui-browser-smoke.md`. Phase 2 integrations remain blocked.
- S2-25 minimal report history frontend page complete: `apps/web/src/pages/ReportHistory.jsx` (mounted at `/reports/history` and gated by the existing `authed` guard) wires the existing read-only `GET /api/v1/reports/runs` and `GET /api/v1/reports/runs/:runId/outputs/:format` endpoints through a new `apps/web/src/lib/reportHistory.js` helper module without touching backend code. The page reuses the existing `AppShell` and adds a `Reports` nav entry between `Reviews` and `Members`. Filters: `status` (select from canonical lifecycle), `report_type` (free text), `report_key` (free text), `date_from`/`date_to` (UTC `YYYY-MM-DD`), `limit` (`1..100`). Per-run cards show `report_name`/`report_key`/`report_type`, status and type badges, scope summary (`organization`/`client`/`location` ids only — never emails), `requested_by_user_id`, `created_at`/`completed_at`, and one row per output with format, status, size (`formatBytes`), storage provider, and a `Download <FORMAT>` button when the output is `succeeded` with `storage_provider` and `storage_key`. The helper module exports `REPORT_RUN_STATUSES`, `REPORT_RUN_FORMATS`, `REPORT_LIST_LIMIT_DEFAULT`/`_MIN`/`_MAX`, `clampReportListLimit`, `buildReportRunsQuery` (omits empty filters, clamps `limit`, drops malformed `status`/`date_*`), `parseContentDispositionFilename` (quoted, bare, and RFC 5987 forms), `safeDownloadFilename` (mirrors the backend `^[A-Za-z0-9._-]+$` allow-list), `formatBytes`, `normalizeReportRunRow` (drops `storage_key` defensively and exposes `downloadable`), `describeReportHistoryError`, `listOrganizationsForReports`, `listReportRunsForUser` (throws when `organization_id` is missing), and `downloadReportOutput` (uses `fetch` directly because the shared JSON `api()` client cannot return raw bytes, sends the existing `Authorization: Bearer …` token from `getToken()`, throws the parsed JSON error envelope on non-2xx). The page combines `downloadReportOutput` with the existing `downloadBlob` helper in `apps/web/src/reportDownloads.js` so DOM operations stay out of the pure helper. 28 new Vitest tests in `apps/web/src/lib/reportHistory.test.js` cover constants, limit clamping, query building, `Content-Disposition` parsing, filename sanitization, byte formatting, error formatting, row normalization (including a `JSON.stringify` scan that confirms `storage_key` is never exposed), and `downloadReportOutput` URL/header construction with an injected fake fetch. Auth/error behavior: real app-auth `401` continues through the existing `api()` redirect-to-login; `403`/`404`/`409`/`500` envelopes display inline through `describeReportHistoryError` and do not clear app auth or invoke Google provider reauth. Out of scope and still future: a frontend detail page, a regenerate button, cursor pagination UI, signed URLs, cloud storage adapters, scheduled reports, email delivery, dedicated `report.run.read`/`report.output.download` audit events, dedicated `report_list`/`report_download` rate-limit buckets, queue/worker-based generation, component-render testing (no `@testing-library/react` installed), and Phase 2 providers. No backend code changed. No dependencies installed. `package-lock.json` unchanged. `apps/api npm test` still reports `tests 176 / pass 176`; `apps/web npm test --run` reports `Test Files 5 passed (5) / Tests 49 passed (49)`; `apps/web npm run build` succeeds (`288 modules transformed`). Proof is recorded in `docs/proof/s2-25-report-history-ui.md`. Phase 2 integrations remain blocked.
- S2-24.1 report output download API live smoke verification complete: a live local API + Mongo smoke under the controlled `s2-15-fixture-org` scope confirmed HTTP 200 from `GET /api/v1/reports/runs/:runId/outputs/{pdf,xlsx}` for `owner` and `admin`, raw PDF (`%PDF` magic) and XLSX (`PK\x03\x04` magic) bodies (not JSON, not base64), `Content-Type` matching the persisted `output.content_type` (`application/pdf` and `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`), `Content-Disposition: attachment` with an ASCII-safe filename derived from `report_key`+`run_id`+`format`, `Content-Length` matching the read buffer length (`2047` for pdf, `8678` for xlsx), `Cache-Control: no-store`, `X-Content-Type-Options: nosniff`, downloaded byte size matching the persisted `output.size`, and downloaded sha256 matching the persisted `output.checksum.value`. Denial coverage exercised manager/viewer against the org-level run (`403 organization_scope_required`), `member` (`403 organization_role_required`), and `invited`/`disabled` (`403 organization_membership_required`). Error coverage exercised `400 bad_request` for invalid (`csv`) and unknown (`json`) format values, `404 report_run_not_found` for a syntactically valid but absent run id, and `401 unauthorized` for the no-auth probe. `404 report_output_not_found`, `409 report_output_not_ready`, `500 report_output_read_failed`, and `500 report_output_integrity_failed` were skipped live because reproducing them safely requires data mutation; they remain covered by the S2-24 unit tests. Because the S2-22.1 storage directory under `/tmp` was wiped between 2026-05-12 and 2026-05-16, one fresh fixture run was generated under the controlled scope (new `report_key: s2-24-1-smoke-dashboard`) before downloading; `report_runs.countDocuments({ organization_id: "s2-15-fixture-org" })` is now `2` (S2-22.1 + S2-24.1). Mongo summary confirmed `path: null`, `storage_provider: local`, relative `storage_key`, `content_type`/`filename`/`checksum.sha256`/`generated_at` set, `expires_at: null`, no `buffer`/`base64`/`input_snapshot` fields, and `location_org_map.countDocuments({ organization_id: "s2-15-fixture-org" })` still `0`. JWT `role` was the default `"individual"` and authorization came from `organization_members` only. Only the API runtime was started; workers and scheduler were not started, and the API was stopped after the smoke. No backend or frontend source code, no test files, no `package.json`, and no `package-lock.json` were changed. Proof is recorded in `docs/proof/s2-24-1-report-output-download-live-smoke.md`. Phase 2 integrations remain blocked.

Near follow-up tasks:

- Report queue/worker/storage/history UI wiring only after the persistence and runtime boundaries are intentionally designed.
- Workspace/member implementation only after the S2-07 audit/design is verified.

## Explicit Boundaries

Do not start Phase 2 integrations. Do not add new providers, new channels, dashboard builder work, billing, entitlement checks, or multi-channel metrics as part of Sprint 2 report foundation tasks.

S2-01 must not generate PDF or XLSX files, persist report records, add report queues/workers, send emails, schedule recurring reports, or expose a public reports API.

S2-02 may generate in-memory PDF buffers only. It must not write files by default, persist report records, add report queues/workers, send emails, schedule recurring reports, expose a public reports API, generate XLSX, or modify frontend export behavior.

S2-03 may generate in-memory XLSX buffers only. It must not write files by default, persist report records, add report queues/workers, send emails, schedule recurring reports, expose a public reports API, modify PDF behavior, or modify frontend export behavior.

S2-04 may add Mongo `reports` and `report_runs` persistence for definitions and lifecycle metadata only. It must not store generated PDF/XLSX buffers, add report queues/workers, send emails, schedule recurring reports, expose a public reports API, modify PDF/XLSX generation behavior, or modify frontend export behavior.

S2-05 may add an authenticated synchronous dashboard snapshot generation route. It must not add report queues/workers, send emails, schedule recurring reports, add file/cloud storage, expose unauthenticated report access, modify frontend export behavior, or start Phase 2 provider/channel work.

S2-06 may add frontend wiring to the existing authenticated dashboard snapshot report route. It must not add backend routes, alter backend generation behavior, add queues/workers, add scheduler changes, add email, add file/cloud storage, add report history UI, remove existing client-side exports without explicit justification, or start Phase 2 provider/channel work.

S2-07 may audit and design the workspace/member foundation only. It must not implement workspace/member APIs, modify auth behavior, modify tenancy/ownership behavior, add RBAC middleware, add frontend workspace/member UI, add billing/entitlements, add migrations, or start Phase 2 provider/channel work. Do not implement workspace/member runtime changes before the S2-07 audit/design is verified.

S2-07.1 may add deterministic local API/web port preparation, generated ignored local env files, docs for local runtime commands, and a restrained app-shell/header cleanup. It must not add workspace/member runtime behavior, change backend business logic beyond local dev env loading/port coordination, remove existing GBP dashboard/report/export/post/review/recurrence functionality, add fake routes, or start Phase 2 integrations.

S2-08 may create `organization_members` indexes and a safe migration that seeds owner memberships from existing `orgs.owner_user_id || orgs.user_id`. S2-08 creates membership data only. It must not change auth/JWT behavior, route authorization behavior, frontend workspace/member UI, RBAC middleware, billing/entitlements, Phase 2 providers, imported Google location binding, `location_org_map` canonicality, or org ownership fields.

S2-09 may add membership-aware organization access helpers and pure tests only. It must not wire those helpers into existing routes, change route authorization behavior, replace current user-owned guards, add RBAC middleware, change JWT/auth behavior, add workspace/member APIs or frontend UI, change Google location binding behavior, make `location_org_map` canonical, or start Phase 2 integrations.

S2-10 is complete. It applied membership-aware checks to low-blast-radius organization and report routes only. It did not modify locations, posts, reviews, recurrence, Google integration routes, provider auth behavior, JWT/auth middleware behavior, frontend workspace/member UI, member-management APIs, invite APIs, billing/entitlements, Phase 2 providers, report PDF/XLSX generation behavior, Google location binding behavior, or `location_org_map` canonicality.

S2-10.1 is complete. It applied membership-aware checks to current GBP location-bound operations after existing owned-location and canonical location scope resolution. It preserved stale/unowned location 404 behavior, provider reauth behavior, app JWT/auth middleware behavior, unbound imported-location fail-closed behavior, report PDF/XLSX generation behavior, Google OAuth/import behavior, and `location_org_map` legacy-only status. It did not add frontend workspace/member UI, member-management APIs, invite APIs, billing/entitlements, queues/workers/scheduler changes, Phase 2 providers, multi-channel metrics, automatic Google location binding, or destructive scripts.

S2-10.2 is complete. It smoke-tested S2-10.1 against the live local API/Mongo environment and produced proof documentation only. It did not change code, provider auth, JWT middleware, frontend behavior, member APIs, invite APIs, billing/entitlements, queues/workers/scheduler behavior, Phase 2 providers, or run destructive scripts.

S2-11 is complete. It added a small backend helper and wired new organization creation to create or preserve an `organization_members` owner record for the authenticated creator. It uses idempotent `{ organization_id, user_id }` behavior compatible with `uniq_organization_members_org_user`, preserves an existing membership's role/status, and fails the org creation request if no owner membership can be created. It did not add member-management APIs, invite APIs, frontend workspace/member UI, RBAC middleware, billing/entitlements, auth/JWT changes, provider auth changes, Phase 2 providers, Google location binding behavior changes, or make `location_org_map` canonical.

S2-11.1 is complete. It smoke-tested S2-11 against the live local API/Mongo environment and produced proof documentation only. It did not change backend code, frontend code, provider auth, JWT middleware, member APIs, invite APIs, billing/entitlements, queues/workers/scheduler behavior, Phase 2 providers, or run destructive scripts.

S2-12 is complete. It added a read-only authenticated organization member listing endpoint under the existing org route. It requires active organization membership and allows only owner/admin/manager members to list sanitized membership rows. It did not add member creation APIs, invite APIs, role update APIs, remove/disable APIs, frontend workspace/member UI, RBAC middleware, billing/entitlements, auth/JWT changes, provider auth changes, report/location behavior changes, Phase 2 providers, Google location binding behavior changes, or make `location_org_map` canonical.

S2-12.1 is complete. It smoke-tested S2-12 against the live local API/Mongo environment and produced proof documentation only. It did not change backend code, frontend code, provider auth, JWT middleware, member mutation APIs, invite APIs, billing/entitlements, queues/workers/scheduler behavior, Phase 2 providers, or run destructive scripts.

S2-13 is complete. It produced a docs-only Sprint 2 workspace/member foundation proof pack and hardening audit. It did not add routes, member-management APIs, invite APIs, role update APIs, remove/disable APIs, frontend workspace/member UI, auth/JWT changes, provider auth changes, report/location behavior changes, RBAC middleware, billing/entitlements, Phase 2 providers, multi-channel metrics, Google location binding changes, or make `location_org_map` canonical.

S2-13.1 is complete. It corrected only the final GPT verification decision in the Sprint 2 workspace/member foundation proof pack after GPT passed the proof. It did not change implementation summaries, access matrices, backend code, frontend code, routes, auth/JWT behavior, provider auth behavior, member APIs, invite APIs, Phase 2 providers, or run destructive scripts.

S2-14 is complete. It produced documentation-only member-management API contract and fixture strategy handoff docs. It did not add backend routes, modify org/member/access services, add member creation APIs, invite APIs, role update APIs, remove/disable APIs, frontend workspace/member UI, auth/JWT changes, provider auth changes, report/location behavior changes, RBAC middleware, billing/entitlements, Phase 2 providers, multi-channel metrics, Google location binding changes, or make `location_org_map` canonical.

S2-15 is complete. It added a dry-run-first fixture seed/audit script, fixture service tests, and proof docs for controlled local `organization_members` fixture records with `s2-15-*` prefixes. Dry-run is default; apply mode is explicit. It did not add backend API routes, member-management APIs, invite APIs, role update APIs, remove/disable APIs, frontend workspace/member UI, auth/JWT changes, provider auth changes, report/location behavior changes, RBAC middleware, billing/entitlements, Phase 2 providers, multi-channel metrics, Google location binding changes, destructive cleanup, or make `location_org_map` canonical.

S2-15.2 is complete. It applied the controlled fixture organization and seven fixture memberships, verified post-apply dry-run state, and produced proof documentation only. It did not change backend source code, frontend code, package files, API routes, auth/JWT behavior, provider auth behavior, report/location behavior, Phase 2 providers, or run destructive cleanup.

S2-16 is complete. It added authenticated direct member-management routes under the existing org router for existing `user_id` based membership creation, patching, and disabling, plus organization member service helpers, tests, and docs. It did not add email invitation delivery, invitation acceptance/token routes, frontend workspace/member UI, auth/JWT middleware changes, provider auth changes, report/location/GBP behavior changes, RBAC middleware, billing/entitlements, Phase 2 providers, multi-channel metrics, Google location binding changes, destructive cleanup, or make `location_org_map` canonical.

S2-16.1 is complete. It smoke-tested S2-16 against the live local API and live MongoDB using only the controlled S2-15 fixture organization and prefix scope, and produced proof documentation. It did not change backend or frontend source code, API routes, member-management services, auth/JWT/provider behavior, report/location/GBP behavior, RBAC middleware, billing/entitlements, Phase 2 providers, Google location binding, or `location_org_map` canonicality, and did not run workers/scheduler or destructive cleanup. It also added a thin Claude Code governance adapter (`CLAUDE.md`, `docs/claude-code/README.md`); `docs/codex/*` remains the current source of truth.

S2-17 is complete. It added a minimal authenticated frontend page at `/organization-members` that wires the existing member-management APIs (`GET/POST /api/v1/orgs/:orgId/members`, `PATCH /api/v1/orgs/:orgId/members/:memberId`, `POST /api/v1/orgs/:orgId/members/:memberId/disable`) through the existing API client, plus a small pure-helper module with unit tests. It did not change backend code, add backend routes, add email invitations, add invitation acceptance/token flows, add billing/entitlements, change auth/JWT/provider behavior, change GBP location binding behavior, change report/location/GBP behavior, make `location_org_map` canonical, or install dependencies.

S2-17.1 is complete. It smoke-tested S2-17 against a local API + web dev-server pair and produced proof documentation only. It did not change backend or frontend source code, API routes, member-management services, auth/JWT/provider behavior, report/location/GBP behavior, RBAC middleware, billing/entitlements, Phase 2 providers, Google location binding, or `location_org_map` canonicality, and did not run workers/scheduler or destructive cleanup. Interactive browser-driven steps remain a recommended manual follow-up; server-side and API-side evidence covered the SPA shell, dev-served page module copy, AppShell nav presence, and the exact endpoints the page calls.

S2-18 is complete. It produced a docs-only Sprint 2 / Phase 1 closeout proof pack and conservative next-task recommendations (`docs/proof/sprint-2-closeout-proof-pack.md`). It did not change backend or frontend source code, API routes, member-management services, auth/JWT/provider behavior, report/location/GBP behavior, RBAC middleware, billing/entitlements, Phase 2 providers, Google location binding, or `location_org_map` canonicality, and did not install dependencies or run workers/scheduler/destructive scripts. Phase 2 integrations remain blocked until the closeout is explicitly accepted.

S2-19 is complete. It added a formal `test` script in `apps/api/package.json` that runs exactly the focused `node --test` matrix already verified in S2-18 (14 test files covering organization members/fixtures/seed-migration/access helpers, org/report routes, report service/PDF/XLSX/store, corsConfig, rate-limit middleware, location binding, and audit log). It did not change backend source code, frontend source code, routes, services, auth/JWT/provider behavior, report/location/GBP runtime behavior, member-management services, RBAC middleware, billing/entitlements, Phase 2 providers, Google location binding behavior, or `location_org_map` canonicality. It did not install dependencies, did not modify `package-lock.json`, did not rename existing scripts, did not add `npm-run-all` usage to the new script, did not run workers/scheduler/destructive scripts, did not start API/worker/scheduler runtime, and did not commit or push. Phase 2 integrations remain blocked.

S2-24 is complete. It adds a read-only `GET /api/v1/reports/runs/:runId/outputs/:format` endpoint that reads the requested PDF/XLSX bytes through the existing `ReportStorageAdapter` and returns them as a raw response body with `Content-Type` from the persisted output, `Content-Disposition: attachment; filename="<sanitized>"`, `Content-Length`, `Cache-Control: no-store`, and `X-Content-Type-Options: nosniff`. Authorization uses `organization_members` only — `owner`/`admin` may download any output for runs in their organization; `manager`/`viewer` may download only when the run's `client_id`/`location_id` matches their `assigned_client_ids`/`assigned_location_ids`; org-level runs are denied for `manager`/`viewer` with `403 organization_scope_required`; `member`/`invited`/`disabled`/missing memberships are denied. JWT `role` and `location_org_map` are not consulted. Output readiness requires `status: succeeded` plus `storage_provider` and `storage_key` (otherwise `409 report_output_not_ready`); missing run ⇒ `404 report_run_not_found`; missing format on the run ⇒ `404 report_output_not_found`; invalid `format` or empty `runId` ⇒ `400 bad_request`; storage read failure ⇒ `500 report_output_read_failed`; size or sha256 mismatch ⇒ `500 report_output_integrity_failed`. The route never returns base64, never returns absolute paths, never logs the storage root, never mutates `report_runs`, and never writes new storage. New backend service helpers `getReportRunById` and `findReportRunOutput` live in `apps/api/src/services/reportStore.js`; `getReportRunById` projects `_id` and `input_snapshot` out of the loaded document and defensively strips both even if the underlying collection ignores Mongo projection. S2-24 did not add a report detail endpoint, the optional regenerate route, frontend code, queues/workers/scheduler changes, storage adapter changes (the existing `readOutput` was sufficient), PDF/XLSX generation changes, signed URLs, cloud storage, dependencies, RBAC middleware, billing/entitlements, Phase 2 providers, Google location binding behavior changes, or `location_org_map` canonicality changes, and did not change `package-lock.json`. Proof is recorded in `docs/proof/s2-24-report-output-download-api.md`. Phase 2 integrations remain blocked.

S2-25.1 is complete. It is documentation/proof only. It ran a local API + web dev-server smoke against the controlled `s2-15-fixture-org` scope, confirming the `/reports/history` page is served by the Vite dev server with the documented route, nav, and page-module wiring (the dev-served `App.jsx`, `AppShell.jsx`, `ReportHistory.jsx`, and `reportHistory.js` modules), the live listing flow (`GET /api/v1/reports/runs?organization_id=s2-15-fixture-org` returns both fixture rows with the documented pagination shape; the documented filter combo narrows to the S2-24.1 row), the live download flow (`GET /api/v1/reports/runs/:runId/outputs/{pdf,xlsx}` returns raw PDF/XLSX bytes whose sha256 prefixes match the S2-24.1 persisted metadata, with `Content-Type` from the persisted output, ASCII-safe `Content-Disposition`, matching `Content-Length`, `Cache-Control: no-store`, and `X-Content-Type-Options: nosniff`), zero `storage_key` exposure in the dev-served page module (the helper module has a single presence-only check), no `_id`/`input_snapshot`/`buffer`/`base64`/absolute-path/env-value/email leakage in the listing response, and the documented inline error envelopes (`400 bad_request`, `401 unauthorized`, `403 organization_role_required`, `403 organization_scope_required`). Interactive in-browser DOM mount and `<a download>` click were skipped because no headless-browser tool is available; the same fetch URL/header contract the page builds was reproduced and validated. Only the API and the Vite dev server were started; workers and scheduler were not. No source code, route, service, dependency, or test file was changed. Proof is recorded in `docs/proof/s2-25-1-report-history-ui-browser-smoke.md`. Phase 2 integrations remain blocked.

S2-25 is complete. It adds a minimal authenticated `/reports/history` frontend page (`apps/web/src/pages/ReportHistory.jsx`) wired through a new pure helper module (`apps/web/src/lib/reportHistory.js`) and its Vitest tests (`apps/web/src/lib/reportHistory.test.js`), plus a new `Reports` nav entry in `apps/web/src/components/AppShell.jsx` and a route in `apps/web/src/App.jsx`. It consumes the existing `GET /api/v1/orgs`, `GET /api/v1/reports/runs`, and `GET /api/v1/reports/runs/:runId/outputs/:format` endpoints, builds download blobs through `fetch` directly because the shared JSON-aware `api()` client cannot return raw bytes, and triggers browser downloads through the existing `downloadBlob` helper in `apps/web/src/reportDownloads.js`. It did not change backend code, API routes, member-management services, auth/JWT/provider behavior, report/location/GBP behavior, RBAC middleware, billing/entitlements, Phase 2 providers, Google location binding behavior, or `location_org_map` canonicality. It did not install dependencies, did not modify `package-lock.json`, did not add a `@testing-library/react` setup, did not run workers/scheduler or destructive scripts, did not commit, and did not push.

S2-24.1 is complete. It is documentation/proof only. It ran a live local API + MongoDB smoke against the controlled `s2-15-fixture-org` scope, confirming `GET /api/v1/reports/runs/:runId/outputs/{pdf,xlsx}` returns HTTP 200 to `owner`/`admin` with raw PDF/XLSX bytes (not JSON, not base64), `Content-Type` matching the persisted `output.content_type`, `Content-Disposition: attachment` with an ASCII-safe filename, `Content-Length` matching the read buffer length, `Cache-Control: no-store`, `X-Content-Type-Options: nosniff`, downloaded byte size matching the persisted `output.size`, downloaded sha256 matching the persisted `output.checksum.value`, the documented denial codes for manager/viewer org-level requests (`403 organization_scope_required`), `member` (`403 organization_role_required`), and `invited`/`disabled` (`403 organization_membership_required`), and the documented error codes for invalid/unknown format (`400 bad_request`), unknown run id (`404 report_run_not_found`), and missing auth (`401 unauthorized`). `404 report_output_not_found`, `409 report_output_not_ready`, `500 report_output_read_failed`, and `500 report_output_integrity_failed` were skipped live because reproducing them safely requires data mutation; they remain covered by the S2-24 unit tests. Because the S2-22.1 on-disk bytes had been wiped from `/tmp`, one fresh fixture run was generated under the controlled scope (`report_key: s2-24-1-smoke-dashboard`) before downloading; Mongo counts and per-output flags were summarized without printing raw documents, and `location_org_map` remained untouched. JWT `role` was the default `"individual"` and authorization came from `organization_members` only. Only the API runtime was started; workers and scheduler were not. No source code, route, service, dependency, or test file was changed. Proof is recorded in `docs/proof/s2-24-1-report-output-download-live-smoke.md`. Phase 2 integrations remain blocked.

S2-23.1 is complete. It is documentation/proof only. It ran a live local API + MongoDB smoke against the controlled `s2-15-fixture-org` scope, confirming `GET /api/v1/reports/runs` returns HTTP 200 with the documented response shape, the S2-22.1 smoke row visible to broad roles, server-controlled newest-first sort, the durable output metadata exposed per row with `path: null`, no `_id`/`input_snapshot`/`buffer`/`base64`/absolute path in the response, `{ status, report_type, report_key, date_from, date_to, limit }` narrowing to the expected single row, and the documented denial codes (`organization_scope_required` for manager/viewer without assigned scope, `organization_role_required` for `member`, `organization_membership_required` for `invited` and `disabled`). Mongo counts matched the listing response, `location_org_map` was untouched, and no JWT, OAuth, encrypted secret, password, email, or raw user record was printed. Only the API runtime was started; workers and scheduler were not. No source code, route, service, dependency, or test file was changed. Proof is recorded in `docs/proof/s2-23-1-report-runs-listing-live-smoke.md`. Phase 2 integrations remain blocked.

S2-23 is complete. It adds a read-only `GET /api/v1/reports/runs` endpoint that returns sanitized `report_runs` rows for an organization, with optional `client_id`/`location_id`/`report_type`/`report_key`/`status`/`date_from`/`date_to` filters and `limit` bounded to `[1,100]` (default `25`). The response shape is `{ report_runs[], pagination: { limit, has_more, next_cursor: null } }`. Sort is server-controlled (`created_at desc` with `id` tiebreaker); client-supplied sort is not accepted. Authorization uses `organization_members` only — `owner`/`admin` see all runs in the org regardless of optional scope filters; `manager`/`viewer` must supply a `client_id` or `location_id` that matches their `assigned_client_ids`/`assigned_location_ids`; `member`/`invited`/`disabled`/missing memberships are denied. JWT `role` and `location_org_map` are not consulted. Sanitization omits Mongo `_id`, raw `input_snapshot`, raw buffers/base64, and absolute server paths. `storage_key` is exposed as durable metadata per the S2-20 contract because no download route exists yet. New backend service helpers `listReportRuns`, `buildReportRunListQuery`, `sanitizeReportRunRow`, `REPORT_LIST_DEFAULT_LIMIT`, `REPORT_LIST_MAX_LIMIT` live in `apps/api/src/services/reportStore.js`. The route mounts under the existing `/api/v1/reports` router and reuses the existing app authentication middleware. S2-23 did not add a report detail endpoint, an output download endpoint, frontend code, queues/workers/scheduler changes, storage adapter changes, PDF/XLSX generation changes, dependencies, or Phase 2 integrations, and did not change `package-lock.json`. Proof is recorded in `docs/proof/s2-23-report-runs-listing-api.md`. Phase 2 integrations remain blocked.

S2-22.1 is complete. It is documentation/proof only. It ran a live local API + MongoDB smoke against the controlled `s2-15-fixture-org` scope, confirming the S2-22 durable storage adapter writes real PDF/XLSX files under `REPORT_STORAGE_LOCAL_DIR` and persists matching `storage_provider`/`storage_key`/`content_type`/`filename`/`size`/`checksum`/`generated_at`/`expires_at` metadata on `report_runs.outputs[]`, with `path: null`, no `input_snapshot`, no raw buffer/base64 in Mongo, and `location_org_map` untouched. The base64 `files[]` response is preserved. Only the API runtime was started; workers and scheduler were not. No source code, route, service, dependency, or test file was changed. Proof is recorded in `docs/proof/s2-22-1-durable-report-storage-live-smoke.md`. Phase 2 integrations remain blocked.

S2-22 is complete. It implements the first cut of the storage adapter designed in S2-20. The `local` `ReportStorageAdapter` lives at `apps/api/src/services/reportStorage.js` with `writeOutput`/`readOutput`/`statOutput`/`deleteOutput`. The synchronous `POST /api/v1/reports/dashboard-snapshot` route writes each successful PDF/XLSX output through the adapter after generation, persists the new durable metadata fields (`storage_provider`, `storage_key`, `content_type`, `filename`, `checksum`, `generated_at`, `expires_at`) on `report_runs.outputs[]` alongside the existing fields, keeps `path` null, never stores buffers in MongoDB, and keeps the response `files[]` base64 shape unchanged. Storage write failure marks the affected output `failed` with `error.code = "report_storage_failed"` and marks the run `failed`, consistent with prior partial-failure behavior; other successful outputs from the same run remain on disk. Storage root resolves from `REPORT_STORAGE_LOCAL_DIR` when set, otherwise from `<os.tmpdir()>/parametrics/report-outputs` so writes never land in the git working tree by default. Tests inject explicit temp directories or fake adapters. The `apps/api` `npm test` script was updated to also run `src/services/reportStorage.test.js`. No new dependencies were added, and `package-lock.json` is unchanged. S2-22 did not add listing/download/regenerate routes, queues, workers, scheduler changes, cloud storage adapters, signed URLs, retention enforcement, report history UI, frontend code changes, auth/JWT/provider changes, RBAC middleware, billing/entitlements, Phase 2 providers, Google location binding behavior changes, or `location_org_map` canonicality changes. Phase 2 integrations remain blocked.

S2-20 is complete. It produced a docs-only report history and durable output storage contract in `docs/architecture/report-history-and-storage.md` with the proof in `docs/proof/s2-20-report-history-storage-contract.md`. The contract describes the future `GET /api/v1/reports/runs`, `GET /api/v1/reports/runs/:runId`, and `GET /api/v1/reports/runs/:runId/outputs/:format` endpoints, an optional future regenerate route, a `ReportStorageAdapter` abstraction with a local-first / cloud-later strategy, additive `report_runs.outputs[]` storage fields (`storage_provider`, `storage_key`, `content_type`, `filename`, `checksum`, `generated_at`, `expires_at`), `organization_members`-based authorization with owner/admin broad access, manager/viewer scoped to assignments, explicit `member`/`invited`/`disabled`/missing denial, a `/reports/history` frontend page recommendation with sanitized columns and no email/raw snapshot display, listing/download index recommendations, `report_list`/`report_download` rate-limit buckets, `report.run.list`/`report.run.read`/`report.output.download` audit events, and the conservative implementation sequence S2-22 storage → S2-23 listing → S2-24 download → S2-25 frontend (with S2-26 queue/worker only after acceptance). S2-20 did not change backend code, frontend code, API routes, services, queues, workers, scheduler behavior, auth/JWT/provider behavior, member-management services, RBAC middleware, billing/entitlements, Phase 2 providers, Google location binding behavior, or `location_org_map` canonicality. It did not install dependencies, modify `package-lock.json`, run workers/scheduler/destructive scripts, start API/worker/scheduler runtime, or commit/push. Phase 2 integrations remain blocked.

Report services should stay testable so later route, queue, worker, scheduler, and frontend tasks can build on a stable metadata contract.

Existing GBP dashboard behavior must not change during S2-01, S2-02, S2-03, S2-04, or S2-05.
