# Sprint 2 Workspace Member Foundation Backlog

This backlog follows the S2-07 workspace/member model audit. It is implementation planning only. Do not implement these tasks until the audit/design is verified.

ParaMetrics remains Google Business Profile first. Phase 2 providers, billing, entitlements, advanced RBAC, report queues/workers/storage/history UI, and multi-channel metrics remain out of scope.

| Task ID | Phase | Priority | Title | Description | Files Likely Affected | Dependencies | Acceptance Criteria | Risks/Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| S2-08 | Phase 1 / Sprint 2 | P0 | Membership collection, indexes, and seed migration | Complete. Added `organization_members` indexes and a safe dry-run/apply migration that creates owner memberships from existing `orgs.owner_user_id || orgs.user_id`. Preserved existing org fields and single-user behavior. | `apps/api/src/startup/ensureIndexes.js`, migration script under `apps/api/src/scripts/`, `apps/api/package.json`, docs/proof | S2-07 verified | Dry-run is default; apply requires explicit flag; creates no memberships without a real org id and owner user id; indexes are verified; no imported Google locations are auto-bound; `location_org_map` remains legacy/readable; focused syntax/tests pass. | Must not infer membership from location rows alone. Must not delete or rewrite org ownership fields. |
| S2-09 | Phase 1 / Sprint 2 | P0 | Membership service/read helpers | Complete. Added testable backend helpers for active membership lookup, role normalization, organization-level permission checks, and simple client/location assignment checks. Did not change route behavior until later tasks. | `apps/api/src/services/organizationAccess.js`, tests, docs | S2-08 | Helpers resolve active membership by `organization_id + user_id`; distinguish owner/admin/manager/viewer/member; deny disabled/invited members; verify manager/viewer assigned client/location scope; unit tests cover allow/deny cases. | Keep helpers narrow. Avoid adding broad RBAC middleware before route needs are known. |
| S2-10 | Phase 1 / Sprint 2 | P0 | Protect org/client/report read paths with membership checks | Complete. Applied membership checks to low-blast-radius org/report paths while preserving current single-user owner behavior from seeded memberships. | `apps/api/src/routes/orgs.js`, `apps/api/src/routes/reports.js`, route/helper tests, docs | S2-09 | Existing owner user behavior still passes; non-member org access fails closed; manager can generate location-scoped reports only for assigned client/location scope; client-sent organization id is not trusted without membership; focused backend route tests pass. | Route behavior changes started here. Scope stayed limited before GBP mutation routes. |
| S2-10.1 | Phase 1 / Sprint 2 | P0 | Protect current GBP location operations with membership checks | Complete. Extended membership checks to location-bound GBP routes after existing owned-location and canonical-scope checks. Preserved provider reauth behavior and current single-user success path. | `apps/api/src/routes/locations.js`, `apps/api/src/routes/posts.js`, `apps/api/src/routes/reviews.js`, `apps/api/src/routes/recurrence.js`, Google performance routes, membership helpers/tests, docs | S2-10 | Owner/admin retain current access; manager can operate assigned scope; viewer can read only where allowed; stale/unassigned location fails closed without app logout; provider reauth does not clear app auth; focused backend tests pass. | Existing user-owned location guard still blocks cross-user shared location access. |
| S2-11 | Phase 1 / Sprint 2 | P0 | New organization owner membership creation | Complete. When a brand-new organization is created through the existing org route, create or preserve an idempotent owner membership for the authenticated creator. Did not add member-management or invite APIs. | `apps/api/src/routes/orgs.js`, `apps/api/src/services/organizationMembers.js`, org route/helper tests, docs | S2-08, S2-09, S2-10 | New org creation creates `role: "owner"` / `status: "active"` membership for `req.user.user_id`; existing membership is not duplicated or downgraded; existing org updates still require owner/admin membership; creation fails before returning success if no owner membership can be created; focused tests pass. | There is still no member-management API or frontend workspace UI. Org and membership writes are not wrapped in a cross-collection transaction. |
| S2-12 | Phase 1 / Sprint 2 | P0 | Read-only organization member listing | Complete. Added an authenticated read-only endpoint under the existing org route to list sanitized organization membership rows for owner/admin/manager members only. Did not add member mutations, invites, or frontend UI. | `apps/api/src/routes/orgs.js`, `apps/api/src/routes/orgs.test.js`, `apps/api/src/services/organizationMembers.js`, `apps/api/src/services/organizationMembers.test.js`, docs | S2-09, S2-10, S2-11 | `GET /api/v1/orgs/:orgId/members` requires active membership in the requested organization; owner/admin/manager can list; viewer/member/invited/disabled/missing membership cannot; response omits `_id`, email, tokens, secrets, passwords, OAuth/provider payloads, and raw user records; list is bounded and deterministically sorted; focused backend tests pass. | Keep endpoint read-only. Frontend workspace/member UI remains a later task. |
| S2-13 | Phase 1 / Sprint 2 | P0 | Workspace membership proof pack and hardening audit | Complete. Audited completed workspace/member implementation and proof through S2-12.1, summarized access behavior, security verification, remaining risks, and next recommended task. | `docs/proof/sprint-2-workspace-member-foundation-proof-pack.md`, guardrails/backlog/docs | S2-10.2, S2-11.1, S2-12.1 | Proof pack includes commands, access matrix results, denied-access coverage, provider/app auth proof references, implementation facts, explicit non-goals, remaining risks, and no Phase 2 drift. | Live role-denial fixture remains absent; keep future verification non-destructive. |
| S2-14 | Phase 1 / Sprint 2 | P0 | Member-management API contract and fixture strategy | Complete. Designed future member-management API contract, role rules, last-owner protection, invite deferral, fixture strategy, and test plan. Documentation only; no runtime behavior changed. | `docs/architecture/member-management-api-contract.md`, `docs/architecture/workspace-members.md`, `docs/backlog/sprint-2-workspace-member-foundation.md`, `docs/codex/sprint-2-phase-1-guardrails.md`, docs/proof | S2-13.1 | Contract documents `POST /api/v1/orgs/:orgId/members`, `PATCH /api/v1/orgs/:orgId/members/:memberId`, `POST /api/v1/orgs/:orgId/members/:memberId/disable`, future invitation route, caller role rules, sanitized responses, safe errors, idempotency, audit events, assignment validation, last-owner protection, fixture strategy, tests, and non-goals. | Email invitation delivery remains future/not implemented. Direct member management by existing `user_id` is the next implementable path. |
| S2-15 | Phase 1 / Sprint 2 | P0 | Controlled membership fixture seed/audit | Recommended. Design or implement a dry-run-first fixture workflow for owner/admin/manager/viewer/member/invited/disabled memberships using obvious local-only prefixes. | docs/proof, optional safe script/tests if approved | S2-14 verified | Fixture workflow is non-destructive by default, prints summarized IDs/counts only, avoids Beetle working data, supports live allow/deny smoke coverage, and documents cleanup/retention. | Do not mutate real memberships. Apply mode, if any, must be explicit and narrowly scoped. |
| S2-16 | Phase 1 / Sprint 2 | P0 | Member-management API implementation | Recommended after fixture strategy. Implement direct member-management routes only after S2-14 and fixture coverage are verified. | `apps/api/src/routes/orgs.js`, `apps/api/src/services/organizationMembers.js`, tests, docs/proof | S2-14 verified, S2-15 fixture strategy accepted | Owner/admin role rules, direct create by `user_id`, patch, disable, last-owner protection, sanitized responses, audit logging, idempotency, assignment validation, and focused route/helper/live smoke tests pass. | Do not add email invitation delivery, frontend UI, Phase 2 providers, RBAC middleware, billing/entitlements, or cross-user Google location access unless separately scoped. |

## Sequencing Notes

S2-08 and S2-09 should happen before route behavior changes. This keeps the membership data shape and helper contract testable before any access-control rewiring.

S2-10 should start with org/client/report paths because report generation already requires explicit organization/client/location scope and has a focused contract. S2-10.1 can then apply the same pattern to broader GBP operations.

S2-11 and S2-12 can be split if API readiness should be verified before frontend work. Email invitation delivery is intentionally not part of Sprint 2.

S2-14 intentionally keeps member-management implementation blocked behind contract verification. The next safest step is fixture seed/audit work so future live smokes can cover owner/admin/manager/viewer/member/invited/disabled behavior without touching Beetle working data.

## Acceptance Gate For The Sequence

Before any implementation task starts:

- S2-07 audit/design must be verified.
- Phase 2 integrations must remain blocked.
- The task must state whether it changes behavior or only adds data/helpers.
- Existing single-user owner behavior must stay green.
- No task may auto-bind imported Google locations.
- No task may make `location_org_map` canonical.
